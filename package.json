{
  "name": "daragh-coll",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "scripts": {
    "dev": "parcel public/index.html --out-dir development -p 3000 ",
    "build-maps": "rimraf ./dist && parcel build public/*.html --out-dir dist --public-url ./",
    "build-prod": "rimraf ./dist && parcel build public/*.html --out-dir dist --no-source-maps  --public-url ./",
    "clean": "rimraf ./development && rimraf -rf ./.cache"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "firebase": "^8.0.0"
  },
  "devDependencies": {
    "@babel/core": "^7.12.3",
    "@babel/plugin-transform-runtime": "^7.12.1",
    "@babel/runtime-corejs3": "^7.12.1",
    "parcel-bundler": "^1.12.4",
    "sass": "^1.27.1"
  }
}
